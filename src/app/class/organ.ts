import { Report } from './report';
export class Organ{
    constructor(
        id: number,
        name: string,
        email: string,
        passoword: string,
        picture: any,
        report: Array<Report>,
    ){}
}